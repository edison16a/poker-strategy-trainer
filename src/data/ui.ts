import raw from "./ui.json";

/**
 * Non-copy UI settings: the raise slider bounds, how long an Elo change
 * flash stays visible, the Champion emoji shown in place of a next-rank
 * badge, the localStorage key and the coach API path.
 */
export const UI = raw;
